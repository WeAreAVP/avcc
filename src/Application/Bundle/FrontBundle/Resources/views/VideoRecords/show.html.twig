{% extends '::base.html.twig' %}

{% block body -%}
    <h1>VideoRecords</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Mediaduration</th>
                <td>{{ entity.mediaDuration }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('record_video') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('record_video_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
